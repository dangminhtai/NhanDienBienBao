# Các bước tiếp theo (Next Steps)

- [ ] **Refactor**: Gộp 2 thư mục `src/` trùng lặp thành một package duy nhất để tuân thủ DRY.
- [ ] **Configuration**: Sử dụng `config.yaml` để quản lý `IMG_SIZE`, `CLASS_NAMES`, và các tham số trích xuất HOG.
- [ ] **UI/UX Enhancement**: Thêm trang Dashboard thống kê chi tiết tập dữ liệu GTSRB bằng `plotly`.
- [ ] **Model Update**: Thử nghiệm thêm các thuật toán khác (CNN, Random Forest) để so sánh hiệu quả với SVM trong phần Minh bạch toán học.
